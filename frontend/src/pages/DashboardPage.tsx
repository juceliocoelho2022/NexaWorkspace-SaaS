import type { Dashboard } from '../types'
import { FolderKanban, Activity, Users, Sparkles } from 'lucide-react'
export function DashboardPage({data}:{data:Dashboard|null}){return <><div className="pageHead"><div><span className="eyebrow">OVERVIEW</span><h1>Olá, {data?.userName||'...'}</h1><p>Indicadores do seu workspace em tempo real.</p></div><span className="planPill">Plano {data?.plan||'...'}</span></div><div className="metrics"><Metric icon={<FolderKanban/>} label="Projetos" value={data?.projects??0}/><Metric icon={<Activity/>} label="Ativos" value={data?.activeProjects??0}/><Metric icon={<Users/>} label="Membros" value={data?.members??0}/><Metric icon={<Sparkles/>} label="Seu papel" value={data?.role||'...'}/></div><div className="grid2"><div className="card"><h3>Arquitetura SaaS</h3><p className="muted">Cada requisição carrega o tenant no token. O backend filtra dados pela empresa autenticada, reduzindo o risco de vazamento entre clientes.</p><div className="flow"><span>JWT</span><b>→</b><span>Tenant ID</span><b>→</b><span>RBAC</span><b>→</b><span>PostgreSQL</span></div></div><div className="card"><h3>Pronto para produção</h3><ul className="plain"><li>✓ Migração de banco com Flyway</li><li>✓ Health check via Actuator</li><li>✓ Auditoria de ações críticas</li><li>✓ Docker Compose full stack</li></ul></div></div></>}
function Metric({icon,label,value}:{icon:React.ReactNode;label:string;value:string|number}){return <div className="metric card"><span className="metricIcon">{icon}</span><div><small>{label}</small><strong>{value}</strong></div></div>}
