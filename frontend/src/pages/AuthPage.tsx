import { useState } from 'react'
import { ArrowRight, CheckCircle2 } from 'lucide-react'
import { api, authStore } from '../services/api'
import { Logo } from '../components/Logo'
export function AuthPage({onDone}:{onDone:()=>void}){const [mode,setMode]=useState<'login'|'register'>('login');const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [form,setForm]=useState({companyName:'Nexa Labs',name:'Jucelio',email:'demo@nexa.com',password:'12345678'});async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const a=mode==='login'?await api.login({email:form.email,password:form.password}):await api.register(form);authStore.set(a);onDone()}catch(err){setError(err instanceof Error?err.message:'Falha')}finally{setBusy(false)}}return <main className="authPage"><section className="authHero"><Logo/><div><span className="eyebrow">MULTI-TENANT B2B SaaS</span><h1>Operação, projetos e crescimento em um único workspace.</h1><p>Arquitetura pronta para escalar: autenticação JWT, RBAC, isolamento por empresa, trilha de auditoria e planos.</p><ul><li><CheckCircle2/>Java 21 + Spring Boot</li><li><CheckCircle2/>React + TypeScript</li><li><CheckCircle2/>PostgreSQL + Flyway</li></ul></div><small>Portfolio-grade architecture • 2026</small></section><section className="authPanel"><form className="card authCard" onSubmit={submit}><h2>{mode==='login'?'Acesse seu workspace':'Crie sua empresa'}</h2><p>{mode==='login'?'Entre para acompanhar sua operação.':'Seu tenant será criado automaticamente.'}</p>{mode==='register'&&<><label>Empresa<input value={form.companyName} onChange={e=>setForm({...form,companyName:e.target.value})}/></label><label>Seu nome<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label></>}<label>E-mail<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Senha<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Processando...':mode==='login'?'Entrar':'Criar workspace'}<ArrowRight/></button><button type="button" className="linkBtn" onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}}>{mode==='login'?'Ainda não tem conta? Criar agora':'Já tenho conta'}</button></form></section></main>}
